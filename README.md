# enumerated types, dependencies

Enums for code input validation. Similar with oneOf validation.
Hit 'F1' key => type dependencies => Click 'Dart: Add dependency' => type name of dependency to add it into yaml file.